{
  "name": "website",
  "version": "0.1.0",
  "scripts": {
    "docs:dev": "vuepress dev docs",
    "docs:build": "vuepress build docs",
    "docs:deploy": "yarn run docs:build && gh-pages -d docs/.vuepress/dist -a"
  },
  "devDependencies": {
    "@vuepress/plugin-pwa": "next",
    "@vuepress/plugin-search": "^2.0.0-beta.25",
    "gh-pages": "^3.1.0",
    "vuepress-vite": "^2.0.0-beta.6"
  }
}
